export interface RentalInput {
    inventory_id: number,
    customer_id: number,
    staff_id: number,
}

export interface RentalOutput{
    rental_id: number,
    inventory_id: number,
    customer_id: number,
    staff_id: number,
}